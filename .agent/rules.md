---
description: Verification Protocol
---
Always verify your code changes before deploying or notifying the user.
1. Run related scripts locally to ensure no errors.
2. Check the output files (HTML/JSON) for correctness and structure.
3. If UI changes are involved, inspect the generated HTML for syntax errors (e.g., onclick handlers).
4. Only push to GitHub after successful verification.
